{
	"name": "synergy",
	"version": "1.0.0",
	"description": "A distributed job scheduler, using Redis.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "James Erringham-Bruce",
	"license": "ISC",
	"dependencies": {
		"node-schedule": "^2.0.0",
		"redis": "^3.0.2",
		"redis-lock": "^0.1.4",
		"util": "^0.12.3"
	},
	"devDependencies": {
		"chai": "^4.3.0",
		"jest": "^26.6.3"
	}
}
